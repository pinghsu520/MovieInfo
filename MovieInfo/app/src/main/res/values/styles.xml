<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/background</item>
    <item name="colorPrimaryDark">@color/background</item>
    <item name="colorAccent">@color/background</item>
  </style>

  <style name="bigLayout" parent="Theme.AppCompat.Light.NoActionBar">
    <!-- Customize your theme here. -->
    <item name="android:background">@color/background</item>
    <item name="android:padding">10dp</item>
  </style>

  <style name="title" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="android:background">@color/textBox</item>
    <item name="android:textSize">45dp</item>
    <item name="android:color">@color/titleText</item>
    <item name="android:textColor">@color/titleText</item>
    <item name="android:gravity">center</item>
  </style>

  <style name="typeHere" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="android:textColor">@color/titleText</item>
    <item name="android:backgroundTint">@color/lightyellow</item>
  </style>

  <style name="button" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="android:background">@color/lightyellow</item>
  </style>

  <style name="swipezone" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="android:background">@color/background</item>
    <item name="android:padding">10dp</item>
  </style>

  <style name="smallTitle" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="android:background">@color/lightyellow</item>
    <item name="android:textSize">23dp</item>
    <item name="android:gravity">center</item>
    <item name="android:textColor">@color/titleText</item>
  </style>

  <style name="text" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="android:background">@color/background</item>
    <item name="android:textColor">@color/titleText</item>
    <item name="android:textSize">15dp</item>
    <item name="android:gravity">left</item>

  </style>


</resources>
